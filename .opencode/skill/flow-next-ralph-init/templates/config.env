# Ralph config (edit as needed)

# Optional epic list (space or comma separated). Empty = scan all open epics.
EPICS=

# Plan gate
REQUIRE_PLAN_REVIEW=0
# PLAN_REVIEW options: rp (RepoPrompt, macOS), opencode (OpenCode), none
PLAN_REVIEW={{PLAN_REVIEW}}

# Work gate
# WORK_REVIEW options: rp (RepoPrompt, macOS), opencode (OpenCode), none
WORK_REVIEW={{WORK_REVIEW}}

# Work settings
BRANCH_MODE=new
MAX_ITERATIONS=25
MAX_ATTEMPTS_PER_TASK=5

# YOLO sets OPENCODE_PERMISSION=* allow (required for unattended runs)
YOLO=1

# UI settings
# RALPH_UI=0  # set to 0 to disable colored output

# Optional OpenCode flags (only used if set)
# FLOW_RALPH_OPENCODE_MODEL=openai/gpt-5.2
# FLOW_RALPH_OPENCODE_VARIANT=high
# FLOW_RALPH_OPENCODE_AGENT=ralph-runner

# Watch mode (command-line flags, not env vars):
#   --watch          Show tool calls in real-time (logs still captured)
#   --watch verbose  Show tool calls + model responses (logs still captured)
# Example: ./ralph.sh --watch
